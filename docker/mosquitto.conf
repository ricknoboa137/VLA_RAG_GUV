# Development broker. Anonymous access is for a trusted LAN only; add a
# password file and TLS before the robot joins any shared network.
listener 1883
protocol mqtt

# WebSocket listener, for browser dashboards and WebXR pages.
listener 9001
protocol websockets

allow_anonymous true
persistence false
